// For Glory — ponto de entrada modular v2.0
// Todo o código está em /static/js/
if (typeof window !== 'undefined') {
    window.__appVersion = '2.0.0';
    console.log('[ForGlory] App modular v2.0 carregado.');
}
