Render recommended commands:
Build Command:
  pip install -r requirements.txt

Start Command:
  alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT

Also set environment variable PYTHON_VERSION=3.12.10 (or rely on .python-version).
